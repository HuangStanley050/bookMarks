import React from "react";
import "./spinner.css";

const Spinner = () => {
    return (
        <div className= "Loader-Wrapper">
           <div className="Loader"/>
         
         </div>
    );
};

export default Spinner;
